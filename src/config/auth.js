/** 
* @paramns options required
* @paramns options.key - required Public API key
* @paramns options.secret - required Private API key
*/


const key = 'key';
const secret = 'secret';

auth = {
  key:key,
  secret:secret
}

module.exports = auth